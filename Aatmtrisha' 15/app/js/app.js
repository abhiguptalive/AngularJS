'use strict';


// Declare app level module which depends on filters, and services
var eventsApp=angular.module('eventsApp', ['ngSanitize']);
/*ng sanitise added here for data dependency because while before our
module load everthing will be in our module whatever in the angular-sannitize.js*/
